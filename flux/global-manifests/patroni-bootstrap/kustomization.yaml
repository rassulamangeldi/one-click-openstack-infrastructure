apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system

resources:
  - job.yml

configMapGenerator:
  - name: ansible-files
    files:
      - inventory.yml
      - playbook.yml
      - vars.yml
      - requirements.yml
