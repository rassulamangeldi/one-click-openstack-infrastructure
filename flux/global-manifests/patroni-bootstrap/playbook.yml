---

- hosts: all
  become: true
  # pre_tasks:
  #   - name: 'ntp | Set Timezone'
  #     timezone:
  #       name: "{{ ntp_timezone }}"
  #     tags:
  #       - ntp
  roles:
    - role: manage-lvm
      tags: lvm

    # - role: ntp
    #   tags: ntp

- hosts: postgres_cluster
  become: true
  roles:

    - role: postgresql-users
      when: is_master | bool
      tags: 
        - never
        - user
